{
  "name": "email-capture-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/server.js",
    "heroku-postbuild": "cd ../join_close_beta && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "body-parser": "^1.19.0",
    "mongodb": "^4.1.0",
    "dotenv": "^10.0.0"
  },
  "engines": {
    "node": "14.x"
  }
}