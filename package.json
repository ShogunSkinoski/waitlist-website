{
  "name": "fintwin360-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node email-capture-backend/src/server.js",
    "heroku-postbuild": "cd join_close_beta && npm install && npm run build && cd ../email-capture-backend && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "create-react-app": "^5.0.1"
  },
  "engines": {
    "node": "14.x"
  }
}
